#include "world.h"


int main(void) {
  
  World test;
  //test.set_state(1);
  test.Resize(4,4);
  
  // test.At(1,1).set_state(1);
  

  //test.At(1,1).set_state(1);
  //std::cout <<  test.At(1,1).get_state() << std::endl;
  

  return 0;
}
